#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
#define dbg(n) cerr << __LINE__ << ": " << #n << " = " << n << '\n'

int n;

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	cin>>n;
}
